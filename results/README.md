# Results

Committed measured artifacts:

- `lexical_retrieval_baseline.json` — measured schema retrieval baseline over 15 custom Chinook questions.
- `build_metadata.json` — artifact-build verification summary.

Future LLM evaluations should use descriptive filenames such as `ollama_qwen25coder7b_lexical_YYYYMMDD.json`. Do not overwrite or report a result without preserving its model, retrieval strategy, dataset, hardware, and date.
